package com.meetline.app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal - Azul Profesional
val Primary = Color(0xFF2563EB)          // Azul profesional
val PrimaryLight = Color(0xFF3B82F6)     // Azul claro
val PrimaryDark = Color(0xFF1D4ED8)      // Azul oscuro
val PrimaryContainer = Color(0xFFDBEAFE) // Contenedor azul suave

// Secundario - Gris Elegante
val Secondary = Color(0xFF475569)        // Gris azulado
val SecondaryLight = Color(0xFF64748B)   // Gris claro
val SecondaryDark = Color(0xFF334155)    // Gris oscuro

// Terciario - Acento Verde
val Tertiary = Color(0xFF059669)         // Verde profesional
val TertiaryLight = Color(0xFF10B981)    // Verde claro

// Superficies
val Surface = Color(0xFFFFFFFF)          // Blanco puro
val SurfaceVariant = Color(0xFFF8FAFC)   // Gris muy suave
val Background = Color(0xFFF1F5F9)       // Fondo gris suave
val BackgroundDark = Color(0xFF0F172A)   // Muy oscuro

// Texto - Colores oscuros para máximo contraste
val OnPrimary = Color(0xFFFFFFFF)        // Blanco sobre primario
val OnSurface = Color(0xFF0F172A)        // Negro para texto principal
val OnSurfaceVariant = Color(0xFF475569) // Gris para texto secundario
val OnBackground = Color(0xFF0F172A)     // Negro para fondo

// Estados
val Success = Color(0xFF059669)          // Verde éxito
val Warning = Color(0xFFD97706)          // Naranja advertencia
val Error = Color(0xFFDC2626)            // Rojo error
val Info = Color(0xFF2563EB)             // Azul info

// Gradientes (colores para Brush)
val GradientStart = Color(0xFF2563EB)
val GradientEnd = Color(0xFF3B82F6)

// Dark Theme
val PrimaryDarkTheme = Color(0xFF3B82F6)
val SurfaceDark = Color(0xFF1E293B)
val SurfaceVariantDark = Color(0xFF334155)
val OnSurfaceDark = Color(0xFFF8FAFC)
val OnSurfaceVariantDark = Color(0xFF94A3B8)

// Legacy (mantener compatibilidad)
val Purple80 = PrimaryLight
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Primary
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)